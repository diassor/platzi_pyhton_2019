# -*- coding: utf-8 -*-

"""
while loop
    ~ Al igual que los for loops los while loops sirven para iterar
        a lo largo de una secuencia.
    @ ej;
    def cuenta_regresiva(n):
        while n > 0:
            print(n)
            n =-1

    ~ Los while pools se ejecutan de la siguiente manera:
        @ Determinan si la condicion es verdadera
        @ Si es verdadera vuelve a ejecutar el loop
        @ Si es falsa, sal del bloque y continua ejecutando el programa

    ~ se tiene que tener mucho cuidado para evitar un infinite loops
    ~ Se puede realizar el keyword break si se quiere salir
        anticipadamente del ciclo



"""
# Nota : los for son usado para largas secuencias
#        El while para una condicion
# convertir el while en lo posible en false para salir del ciclo

# hay veces que se requiere de un valor y no demos seguir ejecutando el
# programa sin el usuario no entrega el valor
# Si generamos un while loop hasta que podamos optener el datos.
